import { PagesPokemonPipe } from './pages-pokemon.pipe';

describe('PagesPokemonPipe', () => {
  it('create an instance', () => {
    const pipe = new PagesPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
